import { NetcashPaymentRequest } from './netcash-payment-request';

describe('NetcashPaymentRequest', () => {
  it('should create an instance', () => {
    expect(new NetcashPaymentRequest()).toBeTruthy();
  });
});
