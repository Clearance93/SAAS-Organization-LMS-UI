import { AdminProfileModel } from './admin-profile-model';

describe('AdminProfileModel', () => {
  it('should create an instance', () => {
    expect(new AdminProfileModel()).toBeTruthy();
  });
});
