import { SchoolDashbaordModel } from './school-dashbaord-model';

describe('SchoolDashbaordModel', () => {
  it('should create an instance', () => {
    expect(new SchoolDashbaordModel()).toBeTruthy();
  });
});
