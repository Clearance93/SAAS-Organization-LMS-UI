import { CourseStreamModel } from './course-stream-model';

describe('CourseStreamModel', () => {
  it('should create an instance', () => {
    expect(new CourseStreamModel()).toBeTruthy();
  });
});
