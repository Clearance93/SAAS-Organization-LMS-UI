import { ExamGradesScaleModel } from './exam-grades-scale-model';

describe('ExamGradesScaleModel', () => {
  it('should create an instance', () => {
    expect(new ExamGradesScaleModel()).toBeTruthy();
  });
});
