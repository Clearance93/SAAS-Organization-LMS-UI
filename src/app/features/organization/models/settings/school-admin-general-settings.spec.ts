import { SchoolAdminGeneralSettings } from './school-admin-general-settings';

describe('SchoolAdminGeneralSettings', () => {
  it('should create an instance', () => {
    expect(new SchoolAdminGeneralSettings()).toBeTruthy();
  });
});
