import { SchoolProfileModel } from './school-profile-model';

describe('SchoolProfileModel', () => {
  it('should create an instance', () => {
    expect(new SchoolProfileModel()).toBeTruthy();
  });
});
