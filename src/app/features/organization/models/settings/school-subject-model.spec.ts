import { SchoolSubjectModel } from './school-subject-model';

describe('SchoolSubjectModel', () => {
  it('should create an instance', () => {
    expect(new SchoolSubjectModel()).toBeTruthy();
  });
});
