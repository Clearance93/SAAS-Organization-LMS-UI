export interface TeacherStream {
  gradeId: string;
  gradeName: string | null;
  streamId: string;
  teacherId: string;
  teacherEmail: string | null;
  streamName: string;
  createdAt: string;
  updatedAt: string;
}